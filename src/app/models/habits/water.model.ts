export interface WaterUnique {
    horas: string[];               // Lista de horas únicas donde se registró el consumo de agua
    cantidades: number[]; 
    fecha_minima : Date   
    fecha_maxima   : Date    
  }
  
